# Server Configuration
server.port=8080
spring.application.name=elegantevents-backend

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/elegantevents?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Clerk Configuration
clerk.webhook.secret=${CLERK_WEBHOOK_SECRET:}

# CORS Configuration (Frontend URL)
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=sebrinasemir2445@gmail.com
spring.mail.password=vnvavibkiyhrssnz

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.from=${MAIL_FROM:sebrinasemir2445@gmail.com}

# Chapa Payment Integration
chapa.secret.key=${CHAPA_SECRET_KEY:}
chapa.public.key=${CHAPA_PUBLIC_KEY:}
chapa.encryption.key=${CHAPA_ENCRYPTION_KEY:}
chapa.webhook.secret=your-webhook-secret-here
chapa.callback.url=http://localhost:8080/api/payments/chapa/callback

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY:}
openai.api.key=${OPENAI_API_KEY}

# Logging
logging.level.com.elegantevents=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG


